<Window x:Class="DemoWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" FontFamily="Times New Roman" Icon="/Icon.ico">
    <Grid>
        <Label x:Name="fioLabel" Content="Label" 
               HorizontalAlignment="Right" 
               Margin="0,10,10,0"
               VerticalAlignment="Top"/>
        <Button x:Name="backButton" Content="Назад" 
                HorizontalAlignment="Left" 
                Margin="10,10,0,0" 
                VerticalAlignment="Top" 
                Height="26" 
                Width="64" 
                Background="{DynamicResource AccentBrush}" Click="backButton_Click"/>

        <ItemsControl d:ItemsSource="{d:SampleData ItemCount=10}">
            <
        </ItemsControl>
        <ListView x:Name="MainListView" d:ItemsSource="{d:SampleData ItemCount=10}" Margin="80,40,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
